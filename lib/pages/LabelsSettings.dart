import 'package:flutter/widgets.dart';

class LabelsSettingsPage extends StatelessWidget {
  const LabelsSettingsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
