import 'package:flutter/widgets.dart';

class NudgerSettings extends StatelessWidget {
  const NudgerSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
